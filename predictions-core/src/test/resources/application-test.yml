spring:
  datasource:
    driver-class-name: 'org.hsqldb.jdbcDriver'
    url: 'jdbc:hsqldb:mem:test;sql.syntax_pgs=true;ignorecase=true;create=true;user=postgres;password=postgres;'
  liquibase:
    default-schema: 'public'
    change-log: 'classpath:/changelog-test.yml'
